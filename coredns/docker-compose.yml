services:
  coredns:
    image: coredns${ENV_SUFFIX}
    container_name: coredns${ENV_SUFFIX}
    build:
      context: ./
    entrypoint: ["./coredns"]
    ports:
      - "53:53/tcp"
      - "53:53/udp"
    networks:
      - bitcoin

