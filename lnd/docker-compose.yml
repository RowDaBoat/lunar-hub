services:
  lnd:
    image: lnd${ENV_SUFFIX}
    container_name: lnd${ENV_SUFFIX}
    build:
      context: ./
    volumes:
      - lnd:/root/.lnd
      - bitcoin:/data
    entrypoint: ["./start.sh"]
    networks:
      - bitcoin
